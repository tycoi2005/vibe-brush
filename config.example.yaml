# Open Brush AI Sculptor — Configuration
# Copy this file to config.yaml and fill in your values.

# LLM Settings (OpenAI-compatible API)
llm:
  api_key: ""                               # set OLLAMA_API_KEY (recommended) or OPENAI_API_KEY
  base_url: "https://ollama.com/v1"        # Ollama Cloud OpenAI-compatible endpoint
  model: "glm-5"                            # model name
  temperature: 0.7                          # creativity (0.0 = precise, 1.0 = creative)
  max_tokens: 4096                          # max response tokens
  # Optional throttling settings (use one or both):
  requests_per_minute: 5                    # -> auto delay of 12s between calls
  # call_delay: 12.0                        # explicit delay override in seconds

# Open Brush Connection
openbrush:
  host: "localhost"
  port: 40074
  command_delay: 0.05    # seconds between API calls (prevent overwhelming OB)

# Export Settings
export:
  auto_save: true        # auto-save sketch after each creation
  auto_export: false     # auto-export to glb after each creation
