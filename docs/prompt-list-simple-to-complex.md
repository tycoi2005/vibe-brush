# Prompt List: Simple to Complex (Open Brush AI Sculptor)

Use these prompts in order, from easy shapes to complex cinematic scenes.
Each prompt has a copy-paste command right below it.

## 0) Ultra Simple

### 0.1 Draw a line
Prompt:
draw a line

Cmd:
```bash
python -m sculptor.cli --prompt "draw a line" -d
```

### 0.2 Draw a dot
Prompt:
draw a dot

Cmd:
```bash
python -m sculptor.cli --prompt "draw a dot" -d
```

### 0.3 Draw a circle
Prompt:
draw a circle

Cmd:
```bash
python -m sculptor.cli --prompt "draw a circle" -d
```

### 0.4 Draw a square
Prompt:
draw a square

Cmd:
```bash
python -m sculptor.cli --prompt "draw a square" -d
```

### 0.5 Draw a triangle
Prompt:
draw a triangle

Cmd:
```bash
python -m sculptor.cli --prompt "draw a triangle" -d
```

### 0.6 Draw a star
Prompt:
draw a star

Cmd:
```bash
python -m sculptor.cli --prompt "draw a star" -d
```

### 0.7 Draw a small sphere
Prompt:
draw a small sphere

Cmd:
```bash
python -m sculptor.cli --prompt "draw a small sphere" -d
```

### 0.8 Draw a small cube
Prompt:
draw a small cube

Cmd:
```bash
python -m sculptor.cli --prompt "draw a small cube" -d
```

### 0.9 Draw a red line
Prompt:
draw a red line

Cmd:
```bash
python -m sculptor.cli --prompt "draw a red line" -d
```

### 0.10 Draw a blue circle
Prompt:
draw a blue circle

Cmd:
```bash
python -m sculptor.cli --prompt "draw a blue circle" -d
```

## 1) Very Simple

### 1. Draw a rose
Prompt:
draw a rose

Cmd:
```bash
python -m sculptor.cli --prompt "draw a rose" -d
```

### 2. Draw a blue cube
Prompt:
draw a blue cube

Cmd:
```bash
python -m sculptor.cli --prompt "draw a blue cube" -d
```

### 3. Draw a glowing circle in the air
Prompt:
draw a glowing circle in the air

Cmd:
```bash
python -m sculptor.cli --prompt "draw a glowing circle in the air" -d
```

### 4. Draw a red heart
Prompt:
draw a red heart

Cmd:
```bash
python -m sculptor.cli --prompt "draw a red heart" -d
```

## 2) Simple Composition

### 5. Draw a small tree on a hill
Prompt:
draw a small tree on a hill

Cmd:
```bash
python -m sculptor.cli --prompt "draw a small tree on a hill" -d
```

### 6. Draw a moon and five stars
Prompt:
draw a moon and five stars

Cmd:
```bash
python -m sculptor.cli --prompt "draw a moon and five stars" -d
```

### 7. Draw a mountain with a river
Prompt:
draw a mountain with a river

Cmd:
```bash
python -m sculptor.cli --prompt "draw a mountain with a river" -d
```

### 8. Draw a sunset over the ocean
Prompt:
draw a sunset over the ocean

Cmd:
```bash
python -m sculptor.cli --prompt "draw a sunset over the ocean" -d
```

## 3) Intermediate 3D Scenes

### 9. Draw a wireframe sphere inside a torus
Prompt:
draw a wireframe sphere inside a torus

Cmd:
```bash
python -m sculptor.cli --prompt "draw a wireframe sphere inside a torus" -d
```

### 10. Create a spiral tower with orbiting rings
Prompt:
create a spiral tower with orbiting rings

Cmd:
```bash
python -m sculptor.cli --prompt "create a spiral tower with orbiting rings" -d
```

### 11. Draw a futuristic bridge over a canyon
Prompt:
draw a futuristic bridge over a canyon

Cmd:
```bash
python -m sculptor.cli --prompt "draw a futuristic bridge over a canyon" -d
```

### 12. Draw a floating island with waterfalls
Prompt:
draw a floating island with waterfalls

Cmd:
```bash
python -m sculptor.cli --prompt "draw a floating island with waterfalls" -d
```

## 4) Advanced Artistic Tasks

### 13. Draw a geometric wolf head with neon cyan accents
Prompt:
draw a geometric wolf head with neon cyan accents

Cmd:
```bash
python -m sculptor.cli --prompt "draw a geometric wolf head with neon cyan accents" -d
```

### 14. Draw a cyberpunk city skyline in rainy night fog
Prompt:
draw a cyberpunk city skyline in rainy night fog

Cmd:
```bash
python -m sculptor.cli --prompt "draw a cyberpunk city skyline in rainy night fog" -d
```

### 15. Draw an ancient temple buried in jungle vines
Prompt:
draw an ancient temple buried in jungle vines

Cmd:
```bash
python -m sculptor.cli --prompt "draw an ancient temple buried in jungle vines" -d
```

### 16. Draw a phoenix made of fire rising from ash
Prompt:
draw a phoenix made of fire rising from ash

Cmd:
```bash
python -m sculptor.cli --prompt "draw a phoenix made of fire rising from ash" -d
```

## 5) Complex Cinematic Scenes

### 17. Draw a giant wolf spirit emerging from mountain mist at twilight with glowing eyes and drifting particles
Prompt:
draw a giant wolf spirit emerging from mountain mist at twilight with glowing eyes and drifting particles

Cmd:
```bash
python -m sculptor.cli --prompt "draw a giant wolf spirit emerging from mountain mist at twilight with glowing eyes and drifting particles" -d
```

### 18. Draw a ruined sci-fi cathedral floating above a stormy ocean with lightning and volumetric fog
Prompt:
draw a ruined sci-fi cathedral floating above a stormy ocean with lightning and volumetric fog

Cmd:
```bash
python -m sculptor.cli --prompt "draw a ruined sci-fi cathedral floating above a stormy ocean with lightning and volumetric fog" -d
```

### 19. Draw a dragon circling a crystal mountain with aurora sky, snow particles, and glowing runes
Prompt:
draw a dragon circling a crystal mountain with aurora sky, snow particles, and glowing runes

Cmd:
```bash
python -m sculptor.cli --prompt "draw a dragon circling a crystal mountain with aurora sky, snow particles, and glowing runes" -d
```

### 20. Draw a mythic battle scene with two colossal spirits, layered depth, atmospheric haze, and cinematic lighting
Prompt:
draw a mythic battle scene with two colossal spirits, layered depth, atmospheric haze, and cinematic lighting

Cmd:
```bash
python -m sculptor.cli --prompt "draw a mythic battle scene with two colossal spirits, layered depth, atmospheric haze, and cinematic lighting" -d
```

## Optional: Interactive Mode

If you want to iterate manually:

```bash
python -m sculptor.cli -d
```

## Copy Task List

```text
1. draw a line
2. draw a dot
3. draw a circle
4. draw a square
5. draw a triangle
6. draw a star
7. draw a small sphere
8. draw a small cube
9. draw a red line
10. draw a blue circle
11. draw a rose
12. draw a blue cube
13. draw a glowing circle in the air
14. draw a red heart
15. draw a small tree on a hill
16. draw a moon and five stars
17. draw a mountain with a river
18. draw a sunset over the ocean
19. draw a wireframe sphere inside a torus
20. create a spiral tower with orbiting rings
21. draw a futuristic bridge over a canyon
22. draw a floating island with waterfalls
23. draw a geometric wolf head with neon cyan accents
24. draw a cyberpunk city skyline in rainy night fog
25. draw an ancient temple buried in jungle vines
26. draw a phoenix made of fire rising from ash
27. draw a giant wolf spirit emerging from mountain mist at twilight with glowing eyes and drifting particles
28. draw a ruined sci-fi cathedral floating above a stormy ocean with lightning and volumetric fog
29. draw a dragon circling a crystal mountain with aurora sky, snow particles, and glowing runes
30. draw a mythic battle scene with two colossal spirits, layered depth, atmospheric haze, and cinematic lighting
```
